import { Routes } from '@angular/router';
import { MaterialStoreComponent } from './material-store.component';

export const materialStoreRoutes: Routes = [
  { path: '', component: MaterialStoreComponent },
];
